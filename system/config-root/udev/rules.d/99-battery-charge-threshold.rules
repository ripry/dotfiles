# Cap charging at 80% to limit battery wear. Replaces the Battery-Health-Charging
# GNOME Shell extension, dropped along with GNOME.
ACTION=="add|change", SUBSYSTEM=="power_supply", KERNEL=="BAT0", ATTR{charge_control_end_threshold}=="?*", ATTR{charge_control_end_threshold}="80"
